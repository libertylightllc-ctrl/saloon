comment on table public.salon_shops is 'Salon Control tenants; access is enforced by row-level security.';
comment on table public.salon_records is 'Tenant-scoped operational records with role-aware row-level security.';
comment on table public.salon_documents is 'Compliance expiry metadata; evidence files live in the private salon-documents bucket.';
