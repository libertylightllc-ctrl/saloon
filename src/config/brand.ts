/**
 * The only place the brand lives. "Salon Control" is a working name until the owner picks the
 * final one (START-HERE, before phase 13). app.json repeats the name until phase 13 moves the
 * Expo config to app.config.ts, which will read it from here.
 */
export const brand = {
  appName: 'Salon Control',
  supportEmail: 'support@example.com',
} as const;
